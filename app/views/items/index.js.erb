console.log('This is from the server, NIIIIIICE. There are <%= @items.size %> items matched')

<% if @items.empty? %>
  $('.items-list-group').html('<%= j(render 'empty_items') %>')
<% else %>
  $('.items-list-group').html('<%= j(render @items) %>')
<% end %>
